{
  "name": "cloudnetes-cms-oauth",
  "version": "1.0.0",
  "private": true,
  "description": "GitHub OAuth broker for Decap CMS (Cloudflare Worker).",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.128.0"
  }
}
